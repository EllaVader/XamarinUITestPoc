namespace MobileFramework.Extensions
{

    public static class XamarinExtensions
    {
        /*        public static void EnterText(this Query element, string text)
                {
                    BasePage.AppContext.ClearText(element);
                    BasePage.AppContext.EnterText(element, text);
                    BasePage.AppContext.DismissKeyboard();
                }

                public static void Tap(this Query element)
                {
                    BasePage.AppContext.Tap(element);
                }

                public static void WaitForElement(this Query element)
                {
                    BasePage.AppContext.WaitForElement(element);
                }*/
    }
}
